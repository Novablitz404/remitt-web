import Image from "next/image";
import Link from "next/link";
import Reveal from "@/components/Reveal";

const rails = ["Stellar", "Circle USDC", "Blend", "GCash", "MoneyGram"];

const featureColumns = [
  {
    title: "Send instantly",
    body: "Pay any @username in about five seconds, for free. No account numbers, no cut-off times, no waiting days.",
  },
  {
    title: "Cash in, cash out",
    body: "Top up and withdraw through the rails people actually use — GCash, MoneyGram, banks, and cards.",
  },
  {
    title: "Earn on idle dollars",
    body: "Move any amount into Earn and collect the live yield of the Blend pool on Stellar. Withdraw anytime.",
  },
];

const activityTags = [
  "Instant P2P",
  "Add cash",
  "Cash out",
  "Contacts",
  "Live activity",
];

const digitalDollars = [
  {
    title: "Watch every cent",
    body: "Your balance, activity, and earnings update live — incoming money appears on its own, no refresh needed.",
  },
  {
    title: "Security that forgives",
    body: "Lose your phone, keep your money. Accounts can be frozen, recovered, and re-keyed — without seed phrases.",
  },
  {
    title: "See your money grow",
    body: "Earn shows the pool's live APY, your earnings so far, and what you make per day. Real numbers, not promos.",
  },
];

const toolCards = [
  {
    title: "Pay anyone",
    body: "Usernames instead of account numbers. Scan a QR or pick a contact.",
  },
  {
    title: "Get started in minutes",
    body: "Pick a username and your account exists. First deposit activates it.",
  },
  {
    title: "Grow your balance",
    body: "One tap moves idle dollars into Earn. One tap brings them back.",
  },
  {
    title: "Works where you are",
    body: "Built for people paid in one country and living in another.",
  },
];

const comparison = [
  { label: "Transfer fee", remitt: "Free", others: "$5–15 + FX markup" },
  { label: "Speed", remitt: "~5 seconds", others: "Hours to days" },
  { label: "Account required", remitt: "Just a username", others: "Bank account + paperwork" },
  { label: "Yield on balance", remitt: "Live APY, no lock-in", others: "0% or locked deposits" },
  { label: "Hidden costs", remitt: "None — fees shown first", others: "Buried in the exchange rate" },
];

const stats = [
  { value: "~5s", label: "Payment settlement" },
  { value: "$0", label: "Fees on sends & cash-outs" },
  { value: "Live", label: "APY read on-chain" },
  { value: "24/7", label: "No banking hours" },
];

const faqs = [
  {
    question: "What is Remitt?",
    answer:
      "Remitt is a dollar account in an app. You can send and receive US dollars instantly with a username, add and withdraw cash through local rails, and earn yield on your balance — no bank account required.",
  },
  {
    question: "Is this crypto? Do I need to know anything about it?",
    answer:
      "Under the hood your balance is USDC, a fully-reserved digital dollar on the Stellar network. You never see gas fees, wallet seeds, or exchange rates — Remitt sponsors every network fee and your balance is always in dollars.",
  },
  {
    question: "What does it cost?",
    answer:
      "Sending money, adding cash, and cashing out are free. The only fee in the product is a 0.5% fee when you withdraw from Earn, and it's shown to you before you confirm.",
  },
  {
    question: "How does Earn work?",
    answer:
      "Earn supplies your dollars to Blend, a lending protocol on Stellar, and passes you its live supply yield. The APY shown in the app is read from the pool in real time, and you can withdraw everything at any moment.",
  },
  {
    question: "When can I use it?",
    answer:
      "Remitt is in private testing on Stellar testnet today. Request early access and we'll reach out as spots open up.",
  },
];

/** CSS-only replica of the app's home screen for the hero mockup. */
function PhoneMockup() {
  return (
    <div className="mx-auto w-[320px] rounded-[44px] border-[10px] border-ink bg-cream p-4 shadow-2xl shadow-ink/30">
      <div className="flex items-center justify-between px-1 pt-1">
        <div className="flex items-center gap-2">
          <div className="flex h-8 w-8 items-center justify-center rounded-full bg-accent text-xs font-black text-white">
            EJ
          </div>
          <span className="text-sm font-bold text-ink">Hi, Erich</span>
        </div>
        <span className="text-sub">▦</span>
      </div>

      <div className="mt-3 rounded-3xl bg-ink p-5">
        <p className="text-xs font-semibold text-white/50">Account balance</p>
        <p className="mt-1 text-3xl font-bold tabular-nums text-white">
          $1,284.50
        </p>
        <p className="mt-1 text-[11px] text-white/50">
          US Dollars · available instantly
        </p>
      </div>

      <div className="mt-3 grid grid-cols-4 gap-2">
        {["Send", "Request", "Add cash", "Cash out"].map((action) => (
          <div key={action} className="flex flex-col items-center gap-1">
            <div className="flex h-11 w-11 items-center justify-center rounded-full bg-accent-soft text-sm font-black text-accent-dark">
              {action[0]}
            </div>
            <span className="text-[10px] font-semibold text-ink">{action}</span>
          </div>
        ))}
      </div>

      <div className="mt-4 rounded-3xl bg-gold-soft p-4">
        <div className="flex items-center justify-between">
          <span className="rounded-full bg-gold px-2 py-0.5 text-[10px] font-black text-white">
            Earn
          </span>
          <span className="text-[11px] font-bold text-accent-dark">+$1.84</span>
        </div>
        <p className="mt-2 text-xl font-bold tabular-nums text-ink">$250.00</p>
        <p className="text-[10px] font-semibold text-sub">
          Earning every day · withdraw anytime
        </p>
      </div>

      <div className="mt-4 space-y-2 pb-2">
        {[
          ["Received · Jann", "+$40.00"],
          ["Sent · Maria", "-$12.50"],
          ["Moved to Earn", "-$250.00"],
        ].map(([label, amount]) => (
          <div
            key={label}
            className="flex items-center justify-between rounded-2xl bg-white px-3 py-2.5"
          >
            <span className="text-[11px] font-semibold text-ink">{label}</span>
            <span
              className={`text-[11px] font-bold tabular-nums ${
                amount.startsWith("+") ? "text-accent-dark" : "text-ink"
              }`}
            >
              {amount}
            </span>
          </div>
        ))}
      </div>
    </div>
  );
}

export default function Home() {
  return (
    <div className="bg-cream">
      {/* Hero — big centered display type over cream, mockup below */}
      <section className="mx-auto max-w-6xl px-4 pb-20 pt-16 text-center sm:px-6 sm:pt-24">
        <Reveal>
          <span className="inline-flex items-center gap-2 rounded-full border border-line bg-white px-4 py-1.5 text-xs font-bold uppercase tracking-[0.14em] text-accent-dark">
            <span className="h-2 w-2 rounded-full bg-accent" />
            Private beta · Stellar testnet
          </span>
        </Reveal>
        <Reveal delay={100}>
          <h1 className="mx-auto mt-6 max-w-4xl font-display text-5xl font-bold leading-[1.02] tracking-tight text-ink sm:text-6xl lg:text-7xl">
            A dollar account that grows with you.
          </h1>
        </Reveal>
        <Reveal delay={200}>
          <p className="mx-auto mt-6 max-w-2xl text-lg leading-8 text-sub">
            Send, receive, and save US dollars instantly — free, in seconds,
            with just a username. No bank required.
          </p>
        </Reveal>
        <Reveal delay={300}>
          <div className="mt-8 flex flex-col items-center justify-center gap-3 sm:flex-row">
            <a
              href="mailto:hello@remitt.app?subject=Early%20access"
              className="inline-flex items-center justify-center rounded-full bg-accent px-7 py-3.5 text-base font-semibold text-white shadow-lg shadow-accent-dark/25 transition-colors hover:bg-accent-dark"
            >
              Get early access
            </a>
            <span className="text-sm font-medium text-sub">
              Free · takes a minute
            </span>
          </div>
        </Reveal>

        <Reveal delay={400}>
          <div className="relative mt-16">
            <div className="absolute inset-x-0 bottom-0 top-24 rounded-[40px] bg-accent-soft" />
            <div className="relative animate-float">
              <PhoneMockup />
            </div>
          </div>
        </Reveal>
      </section>

      {/* Rails strip */}
      <section className="overflow-hidden border-y border-line bg-white py-10">
        <p className="text-center text-xs font-bold uppercase tracking-[0.18em] text-sub">
          Built on rails you can trust
        </p>
        {/* Marquee: the list is doubled and slides by 50% for a seamless loop */}
        <div className="mt-6 flex w-max animate-marquee gap-16 pr-16">
          {[...rails, ...rails, ...rails, ...rails].map((rail, i) => (
            <span
              key={`${rail}-${i}`}
              className="whitespace-nowrap font-display text-lg font-bold tracking-tight text-ink/40"
            >
              {rail}
            </span>
          ))}
        </div>
      </section>

      {/* Three-column features */}
      <section id="features" className="mx-auto max-w-6xl px-4 py-20 sm:px-6">
        <Reveal>
          <h2 className="mx-auto max-w-2xl text-center font-display text-3xl font-bold tracking-tight text-ink sm:text-5xl">
            Smarter money starts here.
          </h2>
        </Reveal>
        <div className="mt-14 grid gap-4 md:grid-cols-3">
          {featureColumns.map((feature, index) => (
            <Reveal key={feature.title} delay={index * 120}>
              <article className="hover-lift h-full rounded-3xl border border-line bg-white p-8 shadow-sm shadow-ink/5">
                <div className="flex h-11 w-11 items-center justify-center rounded-full bg-accent text-sm font-black text-white">
                  0{index + 1}
                </div>
                <h3 className="mt-6 text-2xl font-bold text-ink">
                  {feature.title}
                </h3>
                <p className="mt-3 leading-7 text-sub">{feature.body}</p>
              </article>
            </Reveal>
          ))}
        </div>
      </section>

      {/* Precision section with tag pills (flowfin payroll section) */}
      <section className="mx-auto max-w-6xl px-4 py-8 sm:px-6">
        <Reveal>
        <div className="rounded-[40px] bg-ink p-10 text-white sm:p-16">
          <div className="mx-auto max-w-3xl text-center">
            <h2 className="font-display text-3xl font-bold tracking-tight sm:text-5xl">
              Track every dollar with complete precision.
            </h2>
            <p className="mx-auto mt-5 max-w-xl leading-7 text-white/70">
              Every send, top-up, cash-out, and Earn move in one live feed —
              with the people you pay a tap away.
            </p>
            <div className="mt-8 flex flex-wrap items-center justify-center gap-2">
              {activityTags.map((tag) => (
                <span
                  key={tag}
                  className="rounded-full border border-white/20 bg-white/10 px-4 py-1.5 text-sm font-semibold text-white"
                >
                  {tag}
                </span>
              ))}
            </div>
            <a
              href="mailto:hello@remitt.app?subject=Early%20access"
              className="mt-10 inline-flex items-center justify-center rounded-full bg-accent px-7 py-3.5 font-semibold text-white transition-colors hover:bg-accent-dark"
            >
              Get early access
            </a>
          </div>
        </div>
        </Reveal>
      </section>

      {/* Digital dollars trio */}
      <section id="earn" className="mx-auto max-w-6xl px-4 py-20 sm:px-6">
        <Reveal>
          <h2 className="mx-auto max-w-2xl text-center font-display text-3xl font-bold tracking-tight text-ink sm:text-5xl">
            Unlock the potential of digital dollars.
          </h2>
        </Reveal>
        <div className="mt-14 grid gap-4 md:grid-cols-3">
          {digitalDollars.map((item, index) => (
            <Reveal key={item.title} delay={index * 120}>
              <article className="hover-lift h-full rounded-3xl bg-accent-soft p-8">
                <div className="flex h-10 w-10 items-center justify-center rounded-full bg-accent text-white">
                  ✓
                </div>
                <h3 className="mt-6 text-xl font-bold text-ink">{item.title}</h3>
                <p className="mt-3 leading-7 text-sub">{item.body}</p>
              </article>
            </Reveal>
          ))}
        </div>
      </section>

      {/* Tool cards (2x2) */}
      <section className="mx-auto max-w-6xl px-4 py-8 sm:px-6">
        <div className="grid gap-10 lg:grid-cols-[0.8fr_1.2fr] lg:items-center">
          <Reveal>
            <h2 className="font-display text-3xl font-bold tracking-tight text-ink sm:text-5xl">
              All your money tools in one place.
            </h2>
            <p className="mt-5 max-w-md leading-7 text-sub">
              One app replaces the bank branch, the remittance counter, and the
              savings account — with no queues in between.
            </p>
          </Reveal>
          <div className="grid gap-4 sm:grid-cols-2">
            {toolCards.map((card, index) => (
              <Reveal key={card.title} delay={index * 100}>
                <article className="hover-lift h-full rounded-3xl border border-line bg-white p-6 shadow-sm shadow-ink/5">
                  <h3 className="text-lg font-bold text-ink">{card.title}</h3>
                  <p className="mt-2 text-sm leading-6 text-sub">{card.body}</p>
                </article>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* Comparison table */}
      <section id="how" className="mx-auto max-w-6xl px-4 py-20 sm:px-6">
        <Reveal>
          <h2 className="text-center font-display text-3xl font-bold tracking-tight text-ink sm:text-5xl">
            Why choose Remitt?
          </h2>
        </Reveal>
        <Reveal delay={120}>
        <div className="mx-auto mt-12 max-w-3xl overflow-hidden rounded-3xl border border-line bg-white shadow-sm shadow-ink/5">
          <div className="grid grid-cols-[1.2fr_1fr_1fr] border-b border-line bg-cream px-6 py-4 text-sm font-bold text-ink">
            <span />
            <span className="text-accent-dark">Remitt</span>
            <span className="text-sub">The old way</span>
          </div>
          {comparison.map((row) => (
            <div
              key={row.label}
              className="grid grid-cols-[1.2fr_1fr_1fr] border-b border-line px-6 py-4 text-sm last:border-b-0"
            >
              <span className="font-semibold text-ink">{row.label}</span>
              <span className="font-bold text-accent-dark">{row.remitt}</span>
              <span className="text-sub">{row.others}</span>
            </div>
          ))}
        </div>
        </Reveal>
      </section>

      {/* Testimonial */}
      <section className="mx-auto max-w-6xl px-4 py-8 sm:px-6">
        <Reveal>
        <div className="rounded-[40px] bg-gold-soft p-10 text-center sm:p-16">
          <p className="mx-auto max-w-3xl font-display text-2xl font-bold leading-snug tracking-tight text-ink sm:text-4xl">
            “I sent my brother lunch money from another country and he had it
            before I put my phone down.”
          </p>
          <p className="mt-6 text-sm font-bold text-ink">
            Beta tester
            <span className="font-medium text-sub"> · private testing, 2026</span>
          </p>
        </div>
        </Reveal>
      </section>

      {/* Stats */}
      <section className="mx-auto max-w-6xl px-4 py-20 sm:px-6">
        <Reveal>
          <h2 className="mx-auto max-w-2xl text-center font-display text-3xl font-bold tracking-tight text-ink sm:text-5xl">
            Why your money needs this app.
          </h2>
        </Reveal>
        <div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
          {stats.map((stat, index) => (
            <Reveal key={stat.label} delay={index * 100}>
              <div className="hover-lift h-full rounded-3xl border border-line bg-white p-8 text-center shadow-sm shadow-ink/5">
                <p className="font-display text-4xl font-bold tabular-nums text-accent-dark">
                  {stat.value}
                </p>
                <p className="mt-2 text-sm font-semibold text-sub">
                  {stat.label}
                </p>
              </div>
            </Reveal>
          ))}
        </div>
      </section>

      {/* FAQ */}
      <section id="faq" className="mx-auto max-w-3xl px-4 py-12 sm:px-6">
        <Reveal>
          <h2 className="text-center font-display text-3xl font-bold tracking-tight text-ink sm:text-5xl">
            Questions, answered.
          </h2>
        </Reveal>
        <div className="mt-10 space-y-3">
          {faqs.map((item) => (
            <details
              key={item.question}
              className="group rounded-2xl border border-line bg-white px-5 py-4"
            >
              <summary className="flex cursor-pointer list-none items-center justify-between gap-4 font-bold text-ink">
                {item.question}
                <span className="text-xl text-accent-dark group-open:rotate-45">
                  +
                </span>
              </summary>
              <p className="mt-3 leading-7 text-sub">{item.answer}</p>
            </details>
          ))}
        </div>
      </section>

      {/* Final CTA */}
      <section id="get" className="mx-auto max-w-6xl px-4 py-20 sm:px-6">
        <Reveal>
        <div className="rounded-[40px] bg-accent p-10 text-center text-white sm:p-16">
          <h2 className="mx-auto max-w-2xl font-display text-3xl font-bold tracking-tight sm:text-5xl">
            Be first in line when Remitt opens up.
          </h2>
          <p className="mx-auto mt-5 max-w-xl leading-7 text-white/85">
            Coming soon to iOS and Android. Drop us a line and we&apos;ll save
            you a username.
          </p>
          <a
            href="mailto:hello@remitt.app?subject=Early%20access"
            className="mt-9 inline-flex items-center justify-center rounded-full bg-white px-8 py-4 font-semibold text-accent-dark transition-colors hover:bg-cream"
          >
            Request early access
          </a>
        </div>
        </Reveal>
      </section>

      {/* Footer — dark, flowfin style */}
      <footer className="bg-ink text-white">
        <div className="mx-auto grid max-w-6xl gap-10 px-4 py-14 sm:px-6 md:grid-cols-[1.2fr_0.9fr_0.9fr_0.9fr]">
          <div>
            <Image
              src="/remitt_wht_horizontal.png"
              alt="Remitt"
              width={220}
              height={50}
              className="h-10 w-auto"
            />
            <p className="mt-4 max-w-xs text-sm leading-6 text-white/60">
              Send, receive, and save US dollars instantly. Powered by USDC on
              Stellar.
            </p>
          </div>
          <div>
            <h3 className="text-sm font-bold">Product</h3>
            <div className="mt-4 space-y-3 text-sm text-white/60">
              <Link href="/#features" className="block hover:text-white">
                Features
              </Link>
              <Link href="/#earn" className="block hover:text-white">
                Earn
              </Link>
              <Link href="/#how" className="block hover:text-white">
                Why Remitt
              </Link>
            </div>
          </div>
          <div>
            <h3 className="text-sm font-bold">Company</h3>
            <div className="mt-4 space-y-3 text-sm text-white/60">
              <Link href="/#faq" className="block hover:text-white">
                FAQ
              </Link>
              <a href="mailto:hello@remitt.app" className="block hover:text-white">
                Contact
              </a>
            </div>
          </div>
          <div>
            <h3 className="text-sm font-bold">Rails</h3>
            <div className="mt-4 space-y-3 text-sm text-white/60">
              <span className="block">GCash</span>
              <span className="block">MoneyGram</span>
              <span className="block">Stellar · USDC</span>
            </div>
          </div>
        </div>
        <div className="border-t border-white/10 py-6 text-center text-xs text-white/40">
          © 2026 Remitt · Private beta on Stellar testnet
        </div>
      </footer>
    </div>
  );
}
